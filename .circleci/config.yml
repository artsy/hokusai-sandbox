version: 2.1

orbs:
  hokusai: artsy/hokusai@dev:cd9ecd1d2c1b049a5dfb0ed354bd5dbf

not_staging_or_release: &not_staging_or_release
  filters:
    branches:
      ignore:
        - staging
        - release

only_master: &only_master
  context: hokusai
  filters:
    branches:
      only: master

only_release: &only_release
  context: hokusai
  filters:
    branches:
      only: release

workflows:
  default:
    jobs:
      # pre-staging
      - hokusai/test:
          name: test
          filename: ./hokusai/test.yml.j2
          <<: *not_staging_or_release

      # staging
      - hokusai/push:
          name: push-staging-image
          <<: *only_master
          requires:
            - test

      - hokusai/deploy-staging:
          <<: *only_master
          project-name: hokusai-sandbox
          requires:
            - push-staging-image

      # release
      - hokusai/deploy-production:
          <<: *only_release
